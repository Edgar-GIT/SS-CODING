import { BasicPage } from "@/pages/BasicPage";

export function ProfilePage() {
  return (
    <BasicPage
      eyebrow="Profile"
      title="Track your path through the academy."
      description="Your profile will hold progress, completed chapters, ranking stats, and saved learning paths."
    />
  );
}
